Primary annotation

if we declare @Primary to any class, while dependency injection if an interface has
multiple implementation classess then the primary component will be injected if you dont use 
Qualifier annotation


NOTE: It is recomended to use @Qualifier annotation. 

@Qualifier is having high priority than @Primary